# CLAUDE.md — AntiClaude × AI-OS 專案指引

> 本檔案讓 Claude Code 自動理解專案背景，在對的時候用對的 agent 和 skill。
> **系統版本**：AI-OS 中台架構（Phase 1-5 完成於 2026-03-12）

---

## 專案概述

AntiClaude 是一套個人品牌內容自動化系統，為 Threads 帳號 @sunlee._.yabg 自動產出 AI 相關內容。
現已升級為 **AI-OS 中台架構**，整合 9 個外部 skill 庫，提供統一的能力調度系統。

核心流程：抓取 AI 新聞 → Gemini 分析 → Claude 評分寫文案 → GPT 選題 → Threads 數據追蹤 → 閉環回饋

## 專案架構

```
Anticlaude/                    ← AI-OS 根目錄
├── src/                       ← Python 後端（scrapers, ai, db, tracker, feedback, pipeline）
├── dashboard/                 ← Next.js 14 前端（App Router + Tailwind + recharts）
├── _context/                  ← AI 記憶檔（about_me, workflow, api_reference）
├── _hub/                      ← AI-OS 中台（能力調度核心）
│   ├── registry/              ← 能力索引（skills/agents/tools/capability_map）
│   ├── skills_library/        ← 9 個 clone 的外部 repo
│   ├── shared/
│   │   ├── agents/            ← 7 個 agent（含 5 個新增）
│   │   ├── skills/            ← 9 個 composite skills
│   │   └── tools/             ← data_processing + visualization tools
│   └── visualization/vizro/   ← Vizro 儀表板模板
├── projects/anticlaude/       ← 專案入口（project_context.md）
├── data/                      ← SQLite DB（anticlaude.db）
├── outputs/                   ← 每日報告 / 文案 / Threads 指標 / 週報
└── tests/                     ← pytest 測試
```

## 常用指令

```bash
# 後端 API
cd c:\Users\sun90\Anticlaude
uvicorn src.api.main:app --reload --port 8000

# 前端儀表板
cd c:\Users\sun90\Anticlaude\dashboard
npm run dev

# Pipeline（含 dry-run）
python src/pipeline.py --dry-run
python src/pipeline.py

# 測試
pytest tests/ -v

# DB 初始化
python -m src.db.schema
```

---

## Agent 自動派遣規則

### 任務 → Agent 對照表

根據使用者說的話，**自動載入最適合的 agent**，不用等人指定：

| 使用者說… | 偵測關鍵字 | 載入 Agent | Agent 檔案 |
|----------|-----------|-----------|-----------|
| 「改 UI」「更好看」「調整畫面」 | ui, 設計, 配色, 動畫, CSS, 排版 | **UI Designer + Frontend Developer** | `_hub/shared/agents/agency-agents/` |
| 「寫 API」「加 endpoint」「改後端」 | api, backend, endpoint, 資料庫, DB | **Backend Architect** | `_hub/shared/agents/agency-agents/` |
| 「選題」「今天發什麼」「策略」 | 選題, 策略, 主題, 受眾, 評分 | **Content Strategist** | `_hub/shared/agents/content-strategist.md` |
| 「寫文案」「寫貼文」「Hook」 | 文案, 貼文, Hook, 寫, Threads | **Threads Writer** | `_hub/shared/agents/threads-writer.md` |
| 「成長」「互動率」「數據分析」 | 成長, 分析, 互動, 報告, 趨勢 | **Growth Hacker** | `_hub/shared/agents/agency-agents/` |
| 「部署」「Docker」「CI/CD」 | deploy, docker, ci, 部署, 上線 | **DevOps Automator** | `_hub/shared/agents/agency-agents/` |
| 「做簡報」「投影片」「slides」 | 簡報, slides, 投影片, PPT, deck | **Frontend Slides skill** | `_hub/shared/skills/frontend-slides/` |

### 派遣行為

1. **偵測任務類型**：根據使用者的訊息自動匹配上方表格
2. **載入角色**：讀取對應 agent 的 `.md` 檔案，按其性格和能力回應
3. **載入 Skill**：查 `_hub/registry/capability_map.md`，找到 agent 對應的 skill，讀取 skill 定義
4. **讀取專案上下文**：根據任務讀取相關的 `_context/` 檔案
5. **回報角色**：在回應開頭簡單標註，例如 `[🎨 UI Designer]` 或 `[📝 Threads Writer]`

### 上下文自動載入

| 任務類型 | 自動讀取的檔案 |
|---------|-------------|
| 內容/文案相關 | `_context/about_me.md` + `_context/workflow.md` |
| 數據/分析相關 | `database_schema.md` + `system_architecture.md` |
| 前端 UI 相關 | `dashboard/src/app/globals.css` + `dashboard/src/components/TopNav.tsx` |
| 後端 API 相關 | `src/api/main.py` + `src/config.py` |
| 全部 pipeline | `_context/workflow.md` + `system_architecture.md` |

---

## 重要規則

### 寫程式碼時
- Python：`src/` 目錄，用 async + type hints
- TypeScript：`dashboard/` 目錄，Next.js 14 App Router
- 測試：`tests/` 目錄，pytest
- 所有中文用**繁體中文**，工具名保留英文

### 專案目標
- 受眾：台灣科技工作者
- 主題比例：AI工具實測 40% / 趨勢解讀 25% / 職涯觀點 20% / 個人成長 15%
- 文案風格：朋友聊天，不像上課
- 禁區：不誇大、不全英文、不用翻譯腔

### 閉環系統
- 所有 Threads 數據存 SQLite（`data/anticlaude.db`）
- 回饋分析結果存 `audience_insights` 表
- Claude 評分會自動注入 `audience_insights` 最新數據
- 目標：系統越用越準，越來越懂受眾喜歡什麼

### 工作分工
- **Antigravity** → 規劃、架構、決策、填充 skill/agent 內容
- **Claude Code**（你）→ 寫程式碼、執行任務、調度 skills
- **Codex** → 局部程式碼修復、批量測試生成、CSS 微調
- 收到 `claude_code_*.md` 任務檔 → 照步驟執行

### 執行引擎選擇規則

| 任務類型 | 引擎 | 原因 |
|---------|------|------|
| 跨檔案重構 | **Claude Code** | 需要全局理解 |
| 新增 API endpoint | **Claude Code** | 需要理解架構 |
| 前端新頁面 | **Claude Code** | 需要設計 Token 一致性 |
| Pipeline 修改 | **Claude Code** | 多檔案相依 |
| 簡單 bug fix（單檔） | **Codex** | 快速修復 |
| 生成重複性測試 | **Codex** | 批量工作 |
| CSS / 樣式微調 | **Codex** | 單檔小改動 |
| Docstring 補充 | **Codex** | 機械性工作 |

### AI-OS 資源快速入口
- 能力地圖 → `_hub/registry/capability_map.md`
- Skill 索引 → `_hub/registry/skills_registry.md`
- Agent 索引 → `_hub/registry/agents_registry.md`
- 系統介紹 → `START_HERE.md`（根目錄）
