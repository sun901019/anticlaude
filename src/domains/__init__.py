# src/domains/ — Bounded domain packages (AITOS DDD layer)
# Each sub-package owns its business logic, models, and services.
# Migration from legacy flat modules is gradual — see README_MIGRATION.md in each domain.
