# AntiClaude AI-OS 實作狀態總覽

> 最後對齊：2026-03-13
> 本文件追蹤 Antigravity 規劃 → Claude Code 實作的進度

---

## Agent 映射表（最新狀態）

| 暱稱 | 角色定位 | SOUL.md | 記憶系統 | Pipeline |
|:---:|:---:|:---:|:---:|:---:|
| **Lala**（策略） | 今日選題、策略規劃 | ✅ `content-strategist.md` | ✅ `audience_insights` | ✅ 跑起來了 |
| **Craft**（內容） | Threads 文案撰寫 | ✅ `threads-writer.md` | ✅ `top_hooks` | ✅ 跑起來了 |
| **Ori**（研究） | AI 新聞篩選、台灣切角 | ✅ `market_research_agent.md` | 🔲 `research_cache`（待建） | ✅ 跑起來了 |
| **Lumi**（工程） | 程式碼開發、架構設計 | ✅ `coding_agent.md` | — | ✅ 隨時可用 |
| **Sage**（投資） | 美股/加密/台股分析 | ✅ `sage.md` | 🔲 `investment_log`（待建） | 🔲 待建 |
| **Pixel**（設計） | 視覺風格、UI、品牌 | ✅ `pixel.md` | 🔲 `design_prefs`（待建） | 🔲 待建 |

---

## 工作流模式

| 模式 | 狀態 | 說明 |
|------|------|------|
| **Sequential**（序列） | ✅ 完成 | `orchestrator.py`：抓取→聚類→評分→選題→寫稿，AgentResult 統一交接 |
| **Brainstorming**（辯論） | ✅ 完成 | `debate.py`（穩健派 vs 突破派）+ `judge.py`（裁決），`use_debate=True` 啟動 |
| **Scheduled**（定時） | 🟡 部分 | APScheduler 存在，但未正式與 orchestrator 綁定 |

---

## 記憶系統

| 記憶層 | 狀態 | 說明 |
|--------|------|------|
| **集中記憶模組** | ✅ 完成 | `src/feedback/memory.py`：`get_rich_memory_context()` 統一供所有 Agent 使用 |
| **ScoreAgent 記憶注入** | ✅ 完成 | 類型互動率 + 趨勢 + hashtag → `{audience_insights_section}` |
| **StrategyAgent 記憶注入** | ✅ 完成 | 類型排行 + 策略建議 → `{category_performance}` |
| **WriterAgent 記憶注入** | ✅ 完成（新增） | 高互動 Hook + 最佳 hashtag + 字數 → `{memory_section}` |
| **Ori 研究快取** | 🔲 待建 | `research_cache` 表，避免重複分析同一篇文章 |
| **Sage 投資日誌** | 🔲 待建 | `investment_log` 表，累積分析結論供比對 |
| **Pixel 設計偏好** | 🔲 待建 | `design_prefs` 表 + 品牌色/字體定義 |

---

## 模型路由

| Agent | 模型 | 理由 |
|-------|------|------|
| ClusterAgent | `claude-haiku-4-5` | 模式識別，不需推理 |
| ScoreAgent | `claude-sonnet-4-6` | 複雜多維度判斷 |
| StrategyAgent | `gpt-4o` | 多樣性推理 |
| DebateAgent | `gpt-4o` × 2 | 雙視角並行呼叫 |
| JudgeAgent | `claude-sonnet-4-6` | 細膩中文判斷 |
| WriterAgent | `claude-sonnet-4-6` | 創意寫作 |
| VerifyAgent | `claude-haiku-4-5` | 規則檢查 |

> 所有模型從 `src/config.py` 集中管理，可 `.env` 覆蓋

---

## 4 大應用場景

| 場景 | 狀態 | 說明 |
|------|------|------|
| **內容行銷與社群經營** | ✅ 完整 | Pipeline 全跑通 + 記憶注入 + 辯論機制 |
| **虛擬智庫辯論** | ✅ 完整 | DebateAgent + JudgeAgent 實作 |
| **一人軟體開發** | ✅ 完整 | Lumi（coding-agent + Codex 路由）就位 |
| **個人化投資決策** | 🟡 SOUL 就緒 | Sage SOUL.md 定義完成，pipeline 待建 |

---

## Registry 狀態

| 檔案 | 狀態 |
|------|------|
| `agents_registry.md` | ✅ 更新：6 個核心角色 + 記憶來源 + 狀態 |
| `capability_map.md` | ✅ 更新：執行工具層 + Claude Code vs Codex 決策樹 |
| `tools_registry.md` | ✅ 更新：Codex 已列入 |

---

## 下一步（按優先級）

```
1. 設定 .env API keys → 跑通真實 pipeline（最優先）
2. Threads 數據開始進 DB → audience_insights 開始累積
3. 確認閉環有效後 → 建 Sage 的 investment_log + pipeline
4. Pixel 品牌色 / logo 確認後 → 建 design_prefs
5. APScheduler → 正式綁定 orchestrator（定時排程）
```
